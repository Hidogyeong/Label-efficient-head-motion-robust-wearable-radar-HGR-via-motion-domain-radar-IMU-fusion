# Archive organization record

Primary input: `processing_data.zip` (SHA-256 in `organization_summary.json`).

- 175 input files, including 48 Python files before nested-copy deduplication.
- 50 nested duplicate files mapped to byte-identical outer copies.
- 16 generated bytecode files excluded.
- All unique supplied research source retained; one sample-distribution file was converted from CP949 to UTF-8 without changing decoded text.
- Earlier attachment `Make_Merged_data_v4.py` recovered separately and explicitly labeled; it was not in the current ZIP.
- Original notebook outputs, 17 original logs, 2 local `paths.env` files, and the CP949 source bytes are in the optional private companion archive.
- Public notebook copy retains source but clears saved outputs/execution counts; original notebook is not overwritten.
- No real datasets, checkpoints, SDK binaries, commercial recording software, or font files are included.
- Existing experiment engines, manifests, and original shell scripts are not algorithmically changed.
- Added management tools use separate local paths and timestamped reports; no GitHub action is automatic.

Public-release packaging was tested with a new local Git index: private/runtime locations were not staged. The temporary `.git` used for that packaging test is not distributed. Git history/remote creation is left to the user.

See `docs/KNOWN_ISSUES.md` for scientific/provenance caveats and `docs/VALIDATION.md` for exact testing scope. The main and Korean READMEs are complete language versions of the same archive guide.

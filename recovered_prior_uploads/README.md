# Separately recovered earlier attachment

`Make_Merged_data_v4.py` was not present in the current `processing_data.zip`. Its bytes were recovered from the user's earlier attachment named `Make_Merged_data_v4.py`, which remains mounted in this conversation runtime. It has not been silently treated as part of the new upload.

The source is preserved unchanged and hashed in `provenance/source_manifest.csv`. Confirm that this version produced the final merged dataset. It uses fixed input/output paths; create a working copy before editing. Do not run it on archived data in place.

The current ZIP's `processing_geometry_GPT.py` matches the earlier GPT2 geometry source after newline normalization, so a second identical-text geometry copy was not added here.
